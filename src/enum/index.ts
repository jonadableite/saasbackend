// CampaignPlan enum
export enum CampaignPlan {
  STARTER = "starter",
  GROWTH = "growth",
  SCALE = "scale",
}

// LeadStatus enum
export enum LeadStatus {
  PENDING = "pending",
  SENT = "sent",
  DELIVERED = "delivered",
  READ = "read",
  FAILED = "failed",
}

// MessageType enum
export enum MessageType {
  TEXT = "text",
  IMAGE = "image",
  VIDEO = "video",
  AUDIO = "audio",
  STICKER = "sticker",
}
